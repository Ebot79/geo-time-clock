extends layout

block content
    .container
        form.form-login(role="form")
            h2.form-login-heading Log in
            input.form-control(type="email",placeholder="Email address",required,autofocus)
            input.form-control(type="password",placeholder="Password",required)
            button.btn.btn-lg.btn-primary.btn-block(type="submit") Sign in
        a#newUser(href = '/signUp') new user signup

//sticky footer
    #footer
        .container
            p.text-muted &copy; 2014 TimeClock by Eric Sheasby &amp; Julia Reynolds
